# Required for bill parsing (Groq vision). Get a key at https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Optional: ChromaDB persist directory (default: backend/chroma_db)
# CHROMA_PERSIST_DIR=/path/to/chroma_db
